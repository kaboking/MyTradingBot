name: 🚀 Hammo-Telegram-Only-Engine

on:
  workflow_dispatch: 
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run_notifications:
    runs-on: ubuntu-latest
    steps:
      # 1. فتح المستودع
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      # 2. تجهيز لغة بايثون
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. تثبيت المكتبات المطلوبة للتليجرام
      - name: 📦 Install Requirements
        run: pip install python-telegram-bot

      # 4. تشغيل ملف التليجرام فقط (تم حذف سطر الاستراتيجية)
      - name: 📱 Running Telegram Notifier
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python telegram_bot.py
