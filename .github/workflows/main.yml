name: 🚀 Hammo-Power-Engine-V1

on:
  workflow_dispatch: 

jobs:
  run_everything:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Getting Files Ready
        uses: actions/checkout@v4

      - name: 🐍 Setting Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 🔍 Checking Files Location
        run: ls -R # الخطوة دي هتقول لحمو الملفات فين بالظبط

      - name: 📦 Installing Connection Tools
        run: pip install ccxt python-telegram-bot

      - name: 📡 Linking & Running
        run: python trade.py
