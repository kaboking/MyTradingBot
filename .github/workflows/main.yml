name: 🚀 Hammo-Pure-Engine

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run_trading_system:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repo
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Libraries
        run: pip install ccxt python-telegram-bot

      - name: 📉 Step 1: Running Strategy
        # الموتور بيشغل ملف الاستراتيجية الأول
        run: python trade_logic.py

      - name: 📱 Step 2: Running Telegram Notifier
        # الموتور بيشغل ملف التليجرام بعد ما الاستراتيجية تخلص
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python telegram_bot.py
