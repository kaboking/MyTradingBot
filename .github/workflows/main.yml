name: 🚀 Hammo-Ultimate-Engine

on:
  workflow_dispatch: 

jobs:
  trading_process:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Requirements
        run: pip install ccxt python-telegram-bot

      # السطر ده وظيفته يخلي الموتور يدور على الملف في الفولدر الرئيسي
      - name: 📈 Executing Trade Strategy
        run: |
          if [ -f "trade_logic.py" ]; then
            python trade_logic.py
          else
            echo "❌ يا محمد ملف trade_logic.py مش موجود بره الفولدر!"
            exit 1
          fi

      - name: 📱 Sending Telegram Notification
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python telegram_bot.py


