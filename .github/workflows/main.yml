name: 🚀 Hammo-Pure-Launcher

on:
  workflow_dispatch: 
  schedule:
    - cron: '*/5 * * * *'

jobs:
  launcher:
    runs-on: ubuntu-latest
    steps:
      # 1. فتح الفولدر
      - name: 📂 Checkout
        uses: actions/checkout@v4

      # 2. تجهيز اللغة
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. تحميل الأدوات (ccxt للتداول و telegram للرسائل)
      - name: 📦 Install Tools
        run: pip install ccxt python-telegram-bot

      # 4. استدعاء ملف الاستراتيجية اللي بره (trade_logic.py)
      - name: 📈 Run Strategy File
        run: python trade_logic.py

      # 5. استدعاء ملف التليجرام اللي بره (telegram_bot.py)
      - name: 📱 Run Telegram File
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python telegram_bot.py

