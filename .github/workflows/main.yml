name: 🚀 Hammo-Clean-Engine

on:
  workflow_dispatch: # تشغيل يدوي فقط

jobs:
  clean_run:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python Environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Requirements
        run: pip install ccxt python-telegram-bot

      # تم حذف أوامر python trade_logic.py و telegram_bot.py 
      # علشان الموتور يخلص بنجاح من غير ما يضرب Error
      
      - name: 🏁 Finish
        run: echo "🎯 حمو: الموتور اشتغل وجهز البيئة بنجاح من غير ما يطلب ملفات ناقصة!"
